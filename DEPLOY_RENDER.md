# 🚀 نشر البوت على Render

## خطوات النشر:

### 1️⃣ رفع المشروع على GitHub

```bash
# إذا لم تكن قد رفعت المشروع بعد
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

### 2️⃣ إنشاء حساب على Render

1. اذهب إلى [render.com](https://render.com)
2. سجل حساب جديد (يمكنك استخدام GitHub للتسجيل)

### 3️⃣ نشر البوت

#### الطريقة 1: استخدام render.yaml (الأسهل)

1. في لوحة تحكم Render، اضغط على **"New +"**
2. اختر **"Blueprint"**
3. اتصل بحسابك على GitHub واختر هذا المشروع
4. سيكتشف Render ملف `render.yaml` تلقائياً
5. أضف المتغيرات السرية:
   - `DISCORD_TOKEN`: توكن البوت من Discord Developer Portal
   - `GEMINI_API_KEY`: مفتاح API من Google AI Studio
6. اضغط **"Apply"**

#### الطريقة 2: يدوياً

1. في لوحة تحكم Render، اضغط على **"New +"**
2. اختر **"Background Worker"**
3. اتصل بـ GitHub واختر المشروع
4. أضف الإعدادات التالية:
   - **Name**: `discord-bot-gemini`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
5. في قسم **Environment Variables**:
   - أضف `DISCORD_TOKEN` مع قيمة التوكن
   - أضف `GEMINI_API_KEY` مع قيمة المفتاح
6. اضغط **"Create Background Worker"**

### 4️⃣ الحصول على المفاتيح المطلوبة

#### DISCORD_TOKEN:
1. اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications)
2. اختر البوت الخاص بك
3. اذهب إلى تبويب **"Bot"**
4. انسخ التوكن (أو أعد إنشاءه إذا لزم الأمر)

#### GEMINI_API_KEY:
1. اذهب إلى [Google AI Studio](https://makersuite.google.com/app/apikey)
2. سجل دخول بحساب Google
3. أنشئ مفتاح API جديد
4. انسخ المفتاح

### 5️⃣ مراقبة البوت

- بعد النشر، اذهب إلى صفحة البوت في Render
- اضغط على تبويب **"Logs"** لمراقبة السجلات
- يجب أن ترى رسالة: `✅ تم تسجيل الدخول كـ haven chat ai#1971`

---

## ⚠️ ملاحظات مهمة:

### الخطة المجانية:
- ✅ البوت يعمل 24/7 على الخطة المجانية
- ✅ لا يتوقف عند عدم النشاط (Background Worker)
- ⚠️ قد يكون هناك حد للموارد المتاحة

### تحديث البوت:
- عند رفع تحديثات على GitHub، سيعيد Render نشر البوت تلقائياً
- يمكنك إيقاف هذه الميزة من إعدادات البوت

### استكشاف الأخطاء:

| المشكلة | الحل |
|---------|------|
| البوت لا يعمل | تحقق من السجلات (Logs) في Render |
| خطأ 429 (Resource exhausted) | انتظر قليلاً حتى يتم تجديد حصة Gemini API |
| البوت غير متصل بـ Discord | تحقق من صحة DISCORD_TOKEN |
| لا يرد على الأسئلة | تحقق من صحة GEMINI_API_KEY |

---

## 🎉 تم! البوت الآن يعمل على Render

البوت سيعمل الآن 24/7 ويمكنك استخدامه على Discord في أي وقت!
